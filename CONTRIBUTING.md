# Contributing to the Western Roadtrip map

This is a **private trip companion**, not a multi-tenant product. Don't generalize crew names into a SaaS without asking.

## Prerequisites
- Node.js
- MapTiler key (see README)

## Run
```bash
npm install
cp .env.example .env
npm run dev
```

Keep live-location and Splitwise secrets out of git.
